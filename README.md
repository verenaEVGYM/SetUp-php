# Mini Guestbook (PHP + JSON)

This is a small learning project for a 2-person team.
Goal: build a simple guestbook using PHP and Git.

## Start (local)
Open a terminal in this folder and run:

```bash
php -S 0.0.0.0:8000
